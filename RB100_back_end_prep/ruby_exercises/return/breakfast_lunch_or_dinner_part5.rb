def meal
    'Dinner'
    puts 'Dinner'
end

p meal

# 'Dinner' is outputted, and because 'p' method is used, one 
# can see the raw value returned.  Thus, you also see 'nil' returned
# because `puts` method always returns `nil.`